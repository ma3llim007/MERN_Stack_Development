<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Basic Practice</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form flex justify-center">
            <form action="/create" method="post" class="mt-10 w-[1000px]">
                <div class="flex p-1 w-full flex-col gap-2">
                    <label for="title" class="text-white text-xl font-bold">Title</label>
                    <input required class="w-full py-2 px-2 rounded outline-none text-xl bg-zinc-700 text-white"
                        placeholder="Enter The Title" type="text" name="title" id="title">
                </div>
                <div class="flex p-1 w-full flex-col gap-2">
                    <label for="message" class="text-white text-xl font-bold">Message</label>
                    <textarea required class="w-full py-2 px-2 rounded outline-none resize-none text-xl bg-zinc-700 text-white"
                        placeholder="Enter The Message" type="text" name="message" id="message"></textarea>
                </div>
                <div class="flex p-1 w-[100px] flex-col gap-2">
                    <button
                        class="py-2 px-4 bg-zinc-600 text-white rounded font-bold hover:bg-zinc-400 hover:text-black ease-in duration-300">Submit</button>
                </div>
            </form>
        </div>
        <hr>
        <div class="tasks mt-10 w-[1000px] mx-auto flex justify-center flex-wrap gap-4">
            <% if (files.length > 0) { %>
                <% files.forEach(function(value) { %>
                    <div class="task min-w-[20rem] px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= value.replace('.txt','')  %></h1>
                        <a href="/file/<%= value %>" class="text-blue-500 inline-block font-bold mt-1">View</a>
                        <div class="flex gap-2">
                            <a class="w-[100px] h-[50px] items-center grid justify-items-center text-center bg-red-600 text-white rounded font-bold hover:bg-red-800 ease-in duration-300" href="/delete/<%= value %>" class="inline-block font-bold mt-1">Delete</a>
                            <a class="w-[100px] h-[50px] items-center grid justify-items-center text-center bg-green-600 text-white rounded font-bold hover:bg-green-800 ease-in duration-300" href="/updatefilename/<%= value %>" class="inline-block font-bold mt-1">Update Filename</a>
                            <a class="w-[100px] h-[50px] items-center grid justify-items-center text-center bg-blue-600 text-white rounded font-bold hover:bg-blue-800 ease-in duration-300" href="/updatedetail/<%= value %>" class="inline-block font-bold mt-1">Update Details</a>
                        </div>
                    </div>    
                <% }) %>
            <% } else { %>
                <h1 class="text-white text-center text-4xl font-bold underline">No Task Yet</h1>
            <% } %>
        </div>
    </div>
</body>

</html>